import React from 'react';
import classes from './ContentView.module.css';

const ContentView = (props) => {
    return <div className={classes.ContentView}></div>;
};

export default ContentView;
